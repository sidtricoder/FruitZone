DB_HOST=localhost
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=fruitzone_db
DB_PORT=3306

# Vercel Postgres (set these in Vercel Project Settings)
POSTGRES_URL="postgres://user:password@host:port/database"
# POSTGRES_PRISMA_URL= (if using Prisma)
# POSTGRES_URL_NON_POOLING= (for migrations, etc.)
# POSTGRES_USER=
# POSTGRES_HOST=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=

# Server Port (mainly for local development)
PORT=5001

# For JWT
JWT_SECRET=your_very_strong_jwt_secret_please_change_this
JWT_EXPIRES_IN=1d

# For OTP (optional, if using a third-party service with API keys)
# OTP_SERVICE_API_KEY=
