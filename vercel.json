{
  "version": 2,
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run build",  "rewrites": [
    { 
      "source": "/api/(.*)", 
      "destination": "https://fruit-zone-backend-j7qfnwibu-sidtricoders-projects.vercel.app/api/$1"
    }
  ],
  "github": {
    "silent": true
  }
}
